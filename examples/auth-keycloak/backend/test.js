const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const telemetries = await prisma.telemetry_kv.findMany({ where: { entity_id: '4b344a0e-6277-4711-b9ff-c0623dbfc2dd' }, orderBy: { ts: 'desc' }, take: 50 }); const latestByKey = new Map(); for (const t of telemetries) { if (!latestByKey.has(t.key)) { latestByKey.set(t.key, { key: t.key, value: t.dbl_v !== null ? t.dbl_v : (t.long_v !== null ? Number(t.long_v) : (t.bool_v !== null ? t.bool_v : t.str_v)), lastUpdate: t.ts }); } } console.log(JSON.stringify(Array.from(latestByKey.values()))); } main().finally(() => prisma.$disconnect());
